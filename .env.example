# Airtable Personal Access Token — never commit the real value
AIRTABLE_PAT=your_pat_here

# Restrict CORS to your production app origin (defaults to * if unset)
ALLOWED_ORIGIN=https://your-app-domain.com
